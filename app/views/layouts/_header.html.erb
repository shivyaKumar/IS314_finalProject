<nav class="navbar navbar-expand-lg navbar-dark mb-4" style="background-color: #D30000;">
  <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <div class="container-fluid">
      <div class="d-flex w-100 align-items-center">
        <!-- Left Side with Links and Logo -->
        <ul class="navbar-nav d-flex flex-row me-auto align-items-center">
          <li class="nav-item <%= 'active' if controller_name == 'products' %>">
            <%= link_to 'Products', admin_products_path, class: 'nav-link' %>
          </li>
          <li class="nav-item <%= 'active' if controller_name == 'orders' %>">
            <%= link_to 'Orders', admin_orders_path, class: 'nav-link' %>
          </li>
          <li class="nav-item <%= 'active' if controller_name == 'users' %>">
            <%= link_to 'Users', admin_users_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to 'Sign Out', destroy_admin_admin_session_path, method: :delete, class: 'nav-link' %>
          </li>
          <!-- Centered Logo, moved into the same container -->
          <li class="nav-item">
            <a class="navbar-brand ms-3" href="#">
              <%= image_tag('logo.jpg', alt: 'Oxygen Logo', class: 'logo') %>
            </a>
          </li>
        </ul>
      </div>
    </div>
  </div>
</nav>

<style>
  .navbar {
    background-color: #D30000;
  }

  .navbar-brand {
    width: 100px; /* Adjust the logo size */
    height: auto;
  }

  .navbar-nav {
    margin: 0;
    padding: 0;
    display: flex;
    gap: 1rem; /* Adjust space between links if needed */
  }

  .navbar-nav .nav-link {
    color: white;
    padding: 0.5rem 1rem;
  }

  .navbar-nav .nav-link:hover {
    color: #f8c146;
  }

  .navbar-collapse {
    display: flex;
    align-items: center;
    width: 100% ;
  }

  .container-fluid {
    padding-left: 0;
    padding-right: 0;
  }

  .logo {
    width: 100%; /* Make logo responsive */
    max-width: 100px; /* Adjust the max-width for large screens */
    height: auto;
  }

  @media (max-width: 768px) {
    .logo {
      max-width: 80px; /* Adjust the max-width for medium screens */
    }
  }

  @media (max-width: 576px) {
    .logo {
      max-width: 60px; /* Adjust the max-width for smaller screens */
    }
  }
</style>
