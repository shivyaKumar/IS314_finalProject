<!-- app/views/products/show.html.erb -->

<h1><%= @product.name %></h1>
<p><%= @product.description %></p>
<p>Price: <%= number_to_currency(@product.price) %></p>

<%= link_to 'Add to Wishlist', add_to_wishlist_path(product_id: @product.id), method: :post, class: 'btn btn-wishlist' %>
